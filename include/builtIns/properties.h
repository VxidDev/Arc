#ifndef PROPERTIES_H
#define PROPERTIES_H

#include "../object.h"

Object *builtIn_len_of(Object** args, size_t argCount);
Object* builtIn_is_letter(Object** args, size_t argCount);

#endif // PROPERTIES_H
