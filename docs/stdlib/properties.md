# Properties Library

The `properties` module provides utility functions for inspecting data types.

## Functions

### `is_int(str)`
- **Description:** Attempts to convert a value to an integer. Returns `true` if successful, `false` otherwise.
