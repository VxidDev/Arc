# Type & Properties Library

- `len_of(value)`: Returns the length of a string or list.
- `typeof(value)`: Returns the type of the value (e.g., "number", "string", "list", "function").
- `to_int(value)`: Converts a value to an integer.
